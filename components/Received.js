import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Received = () => {
    return (
        <View>
            <Text style={{color: '#000000'}}>Received Cards</Text>
        </View>
    )
}

export default Received

const styles = StyleSheet.create({})
